import React from 'react';
import PropTypes from 'prop-types';

const Contact = props => {
    return (
        <>
        Contact
        </>
    );
};

Contact.propTypes = {

};

export default Contact;