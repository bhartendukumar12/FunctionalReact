import React from 'react';
import PropTypes from 'prop-types';

const Feature = props => {
    return (
        <>
        Feature
        </>
    );
};

Feature.propTypes = {

};

export default Feature;